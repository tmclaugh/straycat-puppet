---
:backends:
  - eyaml
  - yaml

:logger: console

:hierarchy:
  - fqdn/%{::fqdn}
  - service/cassandra/%{::sc_cassandra_cluster}
  - straycat_dc/%{::straycat_dc}
  - straycat_domain/%{::straycat_domain}
  - common

:yaml:
   :datadir: /etc/puppet/environments/%{environment}/hieradata/

:eyaml:
   :datadir: /etc/puppet/environments/%{environment}/hieradata/
   :extension: 'yaml'
   :gpg_gnupghome: /etc/puppet/keys
